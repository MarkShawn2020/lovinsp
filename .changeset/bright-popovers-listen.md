---
"@lovinsp/core": minor
---

Add sourcePriority for default source selection so projects can lower shadcn/ui or Radix primitive priority while keeping those candidates in the right-click component tree.

Also improves JSX inspector attribute placement for forwarded props and includes the Next.js server-only metadata entry selection fix.
